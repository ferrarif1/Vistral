# Vistral Workshop Assets

Place the provided workshop PNG files in this folder:

- `dataset.png`
- `training.png`
- `exam.png`
- `robot.png`
- `scientist.png`
- `wizard.png`

The `/training-workshop` page renders CSS fallback rooms and characters when any PNG is missing, so the demo remains usable before the final asset pack is copied in.
